@using Southwind.Entities

@using (var tc = Html.TypeContext<ApplicationConfigurationEntity>())
{
    @Html.ValueLine(tc, t => t.Environment)
    <fieldset id="email">
        <legend>Email</legend>
        @Html.EmbeddedControl(tc, c => c.Email)
    </fieldset>
    <fieldset id="sms">
        <legend>SMS</legend>
        @Html.EmbeddedControl(tc, c => c.Sms)
    </fieldset>
}
